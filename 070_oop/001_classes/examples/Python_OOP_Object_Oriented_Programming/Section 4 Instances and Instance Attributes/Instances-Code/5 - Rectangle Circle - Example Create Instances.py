class Rectangle:

    def __init__(self, length, width, color):
        self.length = length
        self.width = width
        self.color = color

my_rectangle = Rectangle(5, 4, "Blue")

class Circle:

    def __init__(self, radius):
        self.radius = radius

my_circle = Circle(8)
