from vendor import Vendor
from vend_cust_adapter import VendCustAdapter

MOCKVENDORS = (
    VendCustAdapter(Vendor('Dough Factory', 1, 'Semolina Court')),
    VendCustAdapter(Vendor('Farm Produce', 14, 'Country Rd.')),
    VendCustAdapter(Vendor('Cocoa World', 53, 'Tropical Blvd.'))
)
