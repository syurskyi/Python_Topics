class Solution:
    ___ twoSum(self, nums: List[int], target: int) -> List[int]:
        m = {}
        n = le.(nums)
        ___ i __ ra..(0,n
            goal = target - nums[i]
            __(goal __ m
                r_ [m[goal], i]
            m[nums[i]] = i
        