class Solution:
    ___ twoSum(self, nums: List[int], target: int) -> List[int]:
        m = {}
        n = le.(nums)
        for i in range(0,n
            goal = target - nums[i]
            __(goal in m
                r_ [m[goal], i]
            m[nums[i]] = i
        