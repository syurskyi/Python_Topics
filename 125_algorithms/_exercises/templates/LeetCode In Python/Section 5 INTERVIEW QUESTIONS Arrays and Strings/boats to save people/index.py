class Solution:
    ___ numRescueBoats(self, people: List[int], limit: int) -> int:
        people.sort()

        left = 0
        right = le.(people)-1

        boats_number = 0

        w___(left<=right
            __(left__right
                boats_number+=1
                break
            __(people[left]+people[right]<=limit
                left+=1

            right-=1
            boats_number+=1
        r_ boats_number