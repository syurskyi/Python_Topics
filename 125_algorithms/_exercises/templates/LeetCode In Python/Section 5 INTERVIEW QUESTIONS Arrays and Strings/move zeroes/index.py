class Solution:
    ___ moveZeroes(self, nums: List[int]) -> None:
        j = 0
        for num in nums:
            __(num != 0
                nums[j] = num
                j += 1

        for x in range(j, le.(nums)):
            nums[x] = 0
