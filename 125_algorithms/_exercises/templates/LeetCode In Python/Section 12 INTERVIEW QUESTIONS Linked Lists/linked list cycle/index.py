class Solution:
    ___ hasCycle(self, head: ListNode) -> bool:
        hare = head
        turtle = head

        w___ turtle and hare and hare.next:
            hare = hare.next.next
            turtle = turtle.next
            __(turtle __ hare
                r_ True
        r_ False
