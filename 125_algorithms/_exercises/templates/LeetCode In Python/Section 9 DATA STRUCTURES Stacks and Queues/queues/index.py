class QueueLine:
	___ __init__(self
        self.q = []
    
    ___ enqueue(self, x: int) -> None:
        self.q.append(x)


    ___ dequeue(self) -> None:
        __(le.(self.q) > 0
            self.q.pop(0)

    ___ front(self) -> int:
        __(le.(self.q) __ 0
            r_ None

        r_ self.q[0]