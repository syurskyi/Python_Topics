___ slices(series, length
    numbers = [i..(digit) ___ digit __ series]
    __ n.. 1 <= length <= l..(numbers
        r.. ValueError("Invalid slice length for this series: " + s..(
            length))
    r.. [numbers[i:i + length] ___ i __ r..(l..(numbers) - length + 1)]
