_______ math


class prime:

    @classmethod
    ___ nth_prime(cls, n):
        primes    # list
        possible = cls.possible_primes()
        while l..(primes) < n:
            x = next(possible)
            __ cls.is_prime(x):
                primes.a..(x)
        r.. primes[n - 1]

    @staticmethod
    ___ is_prime(x):
        ___ i __ r..(2, int(math.sqrt(x)) + 1):
            __ x % i __ 0:
                r.. False
        r.. True

    @staticmethod
    ___ possible_primes():
        yield 2
        n = 3
        while True:
            yield n
            n += 2


___ nth_prime(n):
    r.. prime.nth_prime(n)
