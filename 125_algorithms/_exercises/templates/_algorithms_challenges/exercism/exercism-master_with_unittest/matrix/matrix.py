c_ Matrix:

    ___ - , inp):
        rows = build_rows(inp)
        columns = l..(map(l.., l..(z..(*rows))))

    @staticmethod
    ___ build_rows(inp):
        r.. [[i..(val) ___ val __ row.s.. ] ___ row __ inp.s..('\n')]
