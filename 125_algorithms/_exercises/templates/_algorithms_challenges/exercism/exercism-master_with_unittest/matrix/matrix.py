c_ Matrix:

    ___ - , inp
        rows = build_rows(inp)
        columns = l.. m..(l.., l..(z..(*rows))))

    $
    ___ build_rows(inp
        r.. [[i..(val) ___ val __ row.s.. ] ___ row __ inp.s..('\n')]
