"""Finds the hamming distance between two DNA sequences"""

___ distance(strand_a, strand_b
    """Counts the differences in two sequences of DNA"""
    r_ sum(1 for a,b in zip(strand_a, strand_b) __ a != b)