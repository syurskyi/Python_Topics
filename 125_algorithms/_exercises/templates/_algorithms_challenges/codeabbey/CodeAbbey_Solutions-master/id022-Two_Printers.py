# Python 3.4

___ two_printers
    tests = i..(input())
    answer    # list
    ___ test __ r..(tests):
        x, y, n = [float(x) ___ x __ input().s.. ]
        a = i..(y * n / (x + y))
        b = i..(x * n / (x + y))

        answer_1 = i..(max((a + 1) * x, b * y))
        answer_2 = i..(max(a * x, (b + 1) * y))

        answer.a..(s..(m..(answer_1, answer_2)))
    print(' '.j..(answer))
two_printers()
