___ sortIndexes(amount, values
    answer = []
    sortedValues = sorted(values)
    ___ sortedValue in sortedValues:
        ___ x in range(amount
            __ sortedValue __ values[x]:
                answer.append(str(x+1))
    print(' '.join(answer))
sortIndexes(input(),[int(x) ___ x in raw_input().split()])
