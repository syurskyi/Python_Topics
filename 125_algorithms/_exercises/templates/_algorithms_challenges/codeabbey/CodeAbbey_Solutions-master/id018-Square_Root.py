___ sqRoot(numbers):
    answer    # list
    ___ number __ r..(numbers):
        data = raw_input().s.. 
        value,runs = int(data[0]),int(data[1])
        root = 1.00

        __ root __ 0:
            print(1)
        ____:
            ___ x __ r..(runs):
                division = value / root
                abs(root - division)
                root = (root + division) / 2
            answer.a..(s..(root))
    print(' '.join(answer))
sqRoot(input())
