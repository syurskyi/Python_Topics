___ roundNums(amount):
    answers    # list
    ___ pairCount __ r..(amount):
        pair = raw_input().s..(' ')
        answers.a..(s..(i..(round(float("%.02f" % (float(pair[0]) / float(pair[1])))))))
    print(' '.j..(answers))
roundNums(input())
