___ roundNums(amount
    answers    # list
    ___ pairCount __ r..(amount
        pair = raw_input().s..(' ')
        answers.a..(s..(i..(r..(f__("%.02f" % (f__(pair[0]) / f__(pair[1])))))))
    print(' '.j..(answers))
roundNums(input())
