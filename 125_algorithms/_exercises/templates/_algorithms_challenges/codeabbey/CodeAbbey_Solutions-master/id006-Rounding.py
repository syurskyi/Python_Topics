___ roundNums(amount):
    answers    # list
    ___ pairCount __ r..(amount):
        pair = raw_input().split(' ')
        answers.a..(str(int(round(float("%.02f" % (float(pair[0]) / float(pair[1])))))))
    print(' '.join(answers))
roundNums(input())
