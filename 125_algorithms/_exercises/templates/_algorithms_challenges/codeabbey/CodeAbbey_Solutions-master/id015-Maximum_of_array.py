___ findRange(numbers
    answer    # list
    minNum, maxNum  0,0
    numbers  numbers.s..
    ___ x __ numbers:
        __ i..(x) < i..(minNum
            minNum  x
        __ i..(x) > i..(maxNum
            maxNum  x
    print(s..(maxNum) + " " + s..(minNum
findRange(raw_input
