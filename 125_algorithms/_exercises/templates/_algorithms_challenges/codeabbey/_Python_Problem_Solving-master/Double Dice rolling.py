#accept the range
___ i __ r..(i..(input())):
    #accept the random numbers to be converted to double dice value
    a,b = l..(map(i..,input().s..()))
    #calculate the double dice value
    res = (a % 6 + 1 ) + (b % 6 + 1)
    #print the result
    print(res,end=' ')