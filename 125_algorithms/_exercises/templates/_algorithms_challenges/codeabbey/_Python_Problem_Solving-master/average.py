data = int(input())
result = []

___ i in range(data
    sum_numbs = 0
    avg = 0
    avg_numbs = input().split()
    
    ___ j in range(le.(avg_numbs)-1
        #print('value of avg ',j,' is ',avg_numbs[j])
        sum_numbs = sum_numbs + int(avg_numbs[j])
        #a = [int(x) for x in input().split()]
        #avg = sum(a) / (le.(a) - 1)
        
    avg = sum_numbs / (le.(avg_numbs)-1)
    __ avg.is_integer(
        result.append(int(avg))
    ____
        avg = avg + 0.5
        result.append(int(avg))
        
___ k in result:
    print(k,end=(' '))