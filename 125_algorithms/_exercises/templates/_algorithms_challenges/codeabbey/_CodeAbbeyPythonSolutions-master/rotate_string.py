amount_values i..(input
results    # list

___ rotate(index, s__
    sub_string1 s__[:index]
    sub_string2 s__[index:]

    rotated_string sub_string2+sub_string1
    r.. rotated_string

___ i __ r..(amount_values
    index, s__ map(s.., input().s..
    index i..(index)
    results.a..(rotate(index, s__

print(*results)