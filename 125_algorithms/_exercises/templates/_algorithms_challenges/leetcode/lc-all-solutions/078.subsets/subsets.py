c_ Solution(o..
  ___ subsets  nums
    """
    :type nums: List[int]
    :rtype: List[List[int]]
    """

    ___ dfs(nums, index, path, ans
      ans.a..(path)
      [dfs(nums, i + 1, path + [nums[i]], ans) ___ i __ r..(index, l..(nums))]

    ans    # list
    dfs(nums, 0, [], ans)
    r.. ans
