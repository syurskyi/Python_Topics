class Solution(object):
  ___ optimalDivision(self, nums):
    """
    :type nums: List[int]
    :rtype: str
    """
    __ l..(nums) < 3:
      r.. "/".join(map(s.., nums))
    r.. "%s/(%s)" % (nums[0], "/".join(map(s.., nums[1:])))
