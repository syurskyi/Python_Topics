c_ Solution(object):
  ___ optimalDivision(self, nums):
    """
    :type nums: List[int]
    :rtype: str
    """
    __ l..(nums) < 3:
      r.. "/".j..(map(s.., nums))
    r.. "%s/(%s)" % (nums[0], "/".j..(map(s.., nums[1:])))
