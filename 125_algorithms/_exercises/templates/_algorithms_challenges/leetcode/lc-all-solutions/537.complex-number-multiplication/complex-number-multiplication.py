c_ Solution(o..
  ___ complexNumberMultiply  a, b
    """
    :type a: str
    :type b: str
    :rtype: str
    """
    (ar, ac), (br, bc) = map(i.., a[:-1].s..("+", map(i.., b[:-1].s..("+"
    r.. "{}+{}i".f..(s..(ar * br - ac * bc), s..(ar * bc + br * ac
