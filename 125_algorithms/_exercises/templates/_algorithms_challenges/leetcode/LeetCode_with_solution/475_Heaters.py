c_ Solution o..
    ___ findRadius  houses, heaters
        """
        :type houses: List[int]
        :type heaters: List[int]
        :rtype: int
        """
        heaters = s..(heaters) + [float('inf')]
        i = r = 0
        ___ x __ s..(houses
            # move to next range
            w.. x >= s..(heaters[i:i + 2]) / 2.:
                i += 1
            # ans = hearter - hourse
            r = m..(r, abs(heaters[i] - x))
        r_ r
