c_ Solution:
    ___ selfDividingNumbers  left: int, right: int) -> List[int]:
        # check every digit
        r_ [x ___ x __ r.. left, right+1) __ all([int(i) != 0 and x % int(i)__0 ___ i __ str(x)])]

    # def selfDividingNumbers(self, left: int, right: int) -> List[int]:
    #     return [x for x in range(left, right+1) if all((i and (x % i==0) for i in map(int, str(x))))]
