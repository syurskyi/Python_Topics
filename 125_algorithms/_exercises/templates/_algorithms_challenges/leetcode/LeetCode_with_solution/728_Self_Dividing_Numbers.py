c_ Solution:
    ___ selfDividingNumbers  left: i.., right: i..   List[i..]:
        # check every digit
        r_ [x ___ x __ r.. left, right+1) __ all([i..(i) != 0 a.. x % i..(i)__0 ___ i __ s..(x)])]

    # def selfDividingNumbers(self, left: int, right: int) -> List[int]:
    #     return [x for x in range(left, right+1) if all((i and (x % i==0) for i in map(int, str(x))))]
