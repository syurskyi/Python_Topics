class Solution:
    # @return a tuple, (index1, index2)
    ___ twoSum(self, num, target
        d = {}
        ___ i, e __ enumerate(num
            __ e __ d:
                r_ d[e] + 1, i + 1
            d[target - e] = i
