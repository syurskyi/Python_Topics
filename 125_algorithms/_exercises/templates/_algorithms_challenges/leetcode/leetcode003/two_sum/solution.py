c_ Solution:
    # @return a tuple, (index1, index2)
    ___ twoSum  num, target
        d    # dict
        ___ i, e __ e..(num
            __ e __ d:
                r.. d[e] + 1, i + 1
            d[target - e] i
