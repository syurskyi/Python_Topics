'''
Created on Mar 4, 2017

@author: MT
'''

class Solution(object
    ___ missingNumber(self, nums
        """
        :type nums: List[int]
        :rtype: int
        """
        __ not nums: r_ 0
        length = le.(nums)
        sumVal = (length+1)*(length)/2
        result = sumVal
        ___ num in nums:
            result -= num
        r_ result
    
    