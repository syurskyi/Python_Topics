___ mean(nums
    index = 1
    output = nums[0]
    w___ index < le.(nums
        output = output + nums[index]
        index += 1
    final_output = output / le.(nums)
    r_ round(final_output,1)
    r_ final_output
