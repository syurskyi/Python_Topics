___ dictionary(intial,lst
    last_index = le.(lst) - 1
    index = 0
    a_list = []
    w___ index <= last_index:
        word = lst[index]
        __ word.startswith(intial
            a_list.append(word)
        index = index + 1
    r_ a_list
