___ dictionary(intial,lst
    last_index  l..(lst) - 1
    index  0
    a_list    # list
    w.... index < last_index:
        word  lst[index]
        __ word.startswith(intial
            a_list.a..(word)
        index  index + 1
    r.. a_list
