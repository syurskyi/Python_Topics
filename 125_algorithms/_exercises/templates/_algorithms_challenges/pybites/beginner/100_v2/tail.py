___ tail(filepath, n):
    """Similate Unix' tail -n, read in filepath, parse it into a list,
       strip newlines and return a list of the last n lines"""


    w__ open(filepath,'r') __ f:
       lines = f.readlines()
    


    lines = lines[-n:]


    r.. l..(map(l.... x: x.s..,lines))



