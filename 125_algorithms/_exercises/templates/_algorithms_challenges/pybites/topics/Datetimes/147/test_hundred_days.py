____ d__ _______ d__, t..

____ ? _______ ? ?

OTHER_START_DATE TODAY + t.. d.._55


___ test_get_hundred_weekdays_default_start_date
    days ? start_date_T..
    ... l.. ? __ 100
    # check start and end dates
    ... ? 0 __ T..
    ... ? -1 __ ? 2019, 4, 17
    # check if weekends are not included
    ... ? 1__ ? 2018, 11, 30
    ... ? 2 __ ? 2018, 12, 3
    fri_index ?.i.. ? 2019, 1, 18
    ... ? f.. + 1 __ ? 2019, 1, 21


___ test_get_hundred_weekdays_different_start_date
    days ? start_date_O..
    ... l.. ? __ 100
    # check start and end dates
    ... ? 0 __ O..
    ... ? -1 __ ? 2019, 6, 11
    # check if weekends are not included
    ... ? 2 __ ? 2019, 1, 25
    ... ? 3 __ ? 2019, 1, 28
    fri_index ?.i.. ? 2019, 6, 7
    ... ? f.. + 1 __ ? 2019, 6, 10
