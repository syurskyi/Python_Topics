from unicodedata ______ decomposition


___ filter_accents(text
    """Return a sequence of accented characters found in
       the passed in lowercased text string
    """
    r_ [c ___ c __ text.lower() __ decomposition(c) != '']
