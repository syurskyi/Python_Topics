______ string
from random ______ choices

___ gen_key(*, parts=4, chars_per_part=8
    s = ''
    for part in range(parts
        s += ''.join(choices(string.ascii_uppercase + string.digits,k=chars_per_part))+'-'
    r_ s[:-1]