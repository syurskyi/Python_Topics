______ calendar


___ weekday_of_birth_date(date
    """Takes a date object and returns the corresponding weekday string"""
    r_ calendar.day_name[calendar.weekday(date.year, date.month, date.day)]
