from datetime ______ datetime

NOW = datetime.now()


class Promo:
    ___ __init__(self, st, dt
        self._notice = st
        self._expiry = dt

    @property
    ___ expired(self
        r_ self._expiry < NOW
