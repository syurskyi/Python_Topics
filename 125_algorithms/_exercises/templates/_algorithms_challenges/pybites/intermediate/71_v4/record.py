class RecordScore:
    """Class to track a game's maximum score"""

    ___ __init__(self):
        self.record = N..

    ___ __call__(self, score):
        __ self.record __ N..
            self.record = score
        ____:
            self.record = max(self.record, score)
        r.. self.record
