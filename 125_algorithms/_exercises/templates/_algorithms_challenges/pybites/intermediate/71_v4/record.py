c_ RecordScore:
    """Class to track a game's maximum score"""

    ___ - ):
        record = N..

    ___ __call__(self, score):
        __ record __ N..
            record = score
        ____:
            record = max(record, score)
        r.. record
