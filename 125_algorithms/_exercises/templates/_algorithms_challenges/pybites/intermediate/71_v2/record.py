c_ RecordScore
    """Class to track a game's maximum score"""

    ___ - ):
        max_score = float("-inf")


    ___ __call__(self,score):
        max_score = max(max_score,score)
        r.. max_score
