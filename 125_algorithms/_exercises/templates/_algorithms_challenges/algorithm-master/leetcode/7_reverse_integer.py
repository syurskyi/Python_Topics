class Solution:
    ___ reverse(self, x
        """
        :type x: int
        :rtype: int
        """
        ans = 0
        __ not x:
            r_ ans

        INT_MAX = 0x7FFFFFFF
        _x = x __ x > 0 else -x

        w___ _x:
            ans = ans * 10 + _x % 10
            _x //= 10

        __ ans >= INT_MAX:
            r_ 0

        r_ ans __ x > 0 else -ans
