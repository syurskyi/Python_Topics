c_ Solution:
    ___ countAndSay  N
        """
        :type N: int
        :rtype: str
        """
        queue = '1'

        __ n.. N:
            r.. queue

        _queue    # list

        ___ _ __ r..(N - 1
            cnt = 0
            char = queue[0]

            ___ c __ queue:
                __ c __ char:
                    cnt += 1
                    _____
                _queue.extend((s..(cnt), char
                cnt = 1
                char = c

            _queue.extend((s..(cnt), char
            queue, _queue = ''.j..(_queue), []

        r.. queue
