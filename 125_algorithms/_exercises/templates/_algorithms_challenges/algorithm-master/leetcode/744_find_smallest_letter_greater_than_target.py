class Solution:
    ___ nextGreatestLetter(self, L, target
        """
        :type L: List[str]
        :type target: str
        :rtype: str
        """
        n = le.(L)
        left, right = 0, n - 1

        w___ left + 1 < right:
            mid = (left + right) // 2
            __ L[mid] <= target:
                left = mid
            ____
                right = mid

        __ target < L[left]:
            r_ L[left]

        __ target < L[right]:
            r_ L[right]

        r_ L[0]
