class Solution:
    ___ repeatedString(self, a, b
        """
        :type a: str
        :type b: str
        :rtype: int
        """
        __ le.(b) <= le.(a) and b in a:
            r_ 1
        __ not a or not b:
            r_ -1

        ans = b.count(a)
        c = b.split(a)

        __ c[0] and a.endswith(c[0]
            ans += 1

        __ c[-1] and a.startswith(c[-1]
            ans += 1

        r_ ans __ a.startswith(c[-1]) and a.endswith(c[0]) else -1
