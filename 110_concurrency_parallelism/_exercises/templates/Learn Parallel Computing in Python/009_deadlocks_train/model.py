c_ Train:
    ___  -   uid, train_length, front):
        uid = uid
        train_length = train_length
        front = front


c_ Intersection:
    ___  -   uid, mutex, locked_by):
        uid = uid
        mutex = mutex
        locked_by = locked_by


c_ Crossing:
    ___  -   position, intersection):
        position = position
        intersection = intersection
